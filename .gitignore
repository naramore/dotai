# Secrets — never commit actual config with tokens
opencode.json
settings.json

# Node
node_modules/
package-lock.json
bun.lock

# OS
.DS_Store
*.swp
*~

# State files (machine-specific)
opencode-notifier-state.json
opencode.json.bak
settings.json.bak
